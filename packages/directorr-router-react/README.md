# directorr-router-react

>Directorr router bindings for [React](https://github.com/facebook/react).

## Installation

Run `npm install @nimel/directorr-router-react --save` or `yarn add @nimel/directorr-router-react`
