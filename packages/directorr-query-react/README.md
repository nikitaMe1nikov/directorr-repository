# directorr-query-react

>Binding directorr-query for [React](https://github.com/facebook/react).

## Installation

Run `npm install @nimel/directorr-query-react --save` or `yarn add @nimel/directorr-query-react`
